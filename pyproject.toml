[project]
name = "paca-matrix"
version = "0.1.0"
requires-python = ">=3.10.12"

dependencies = [
    "python-dotenv~=1.0",
    "matrix-nio[e2e]>=0.24",
    "aiohttp~=3.9",
    "setproctitle>=1.3",
]

[project.optional-dependencies]
dev = [
    "black",
    "isort",
    "mypy~=1.9",
    "pytest",
    "pytest-asyncio",
]

[project.scripts]
paca = "paca_matrix.__main__:main"

[build-system]
requires = ["setuptools >= 61.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["paca_matrix"]

[tool.isort]
profile = "black"
skip = [".direnv", "result", ".venv"]

[tool.pyright]
typeCheckingMode = "strict"
reportMissingTypeStubs = "none"
exclude = [".direnv", "result", ".venv"]
extraPaths = ["."]

# Disable private usage warnings for tests since they need to test internal methods
executionEnvironments = [
  { root = "tests", reportPrivateUsage = false }
]

[tool.mypy]
strict = true
exclude = [".direnv", "result", ".venv"]

# Improved error messages
pretty = true
show_column_numbers = true
show_error_context = true

[[tool.mypy.overrides]]
module = ["nio"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"

# [tool.setuptools.package-data]
# # Uncomment to include non-Python files in your package distribution
# # Example: include all .txt files from any package directory
# "*" = [
#     "*.txt",
# ]
